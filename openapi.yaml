openapi: 3.0.3
info:
  title: API Distribuidora
  version: '1.0.0'
  description: API para gestionar clientes (clientes de garrafones). Endpoints para listar, crear, actualizar y eliminar clientes.
servers:
  - url: http://localhost:3000
    description: Servidor local
paths:
  /api/clientes:
    get:
      summary: Listar clientes
      description: Devuelve la lista de clientes.
      responses:
        '200':
          description: Lista de clientes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Cliente'
        '500':
          description: Error del servidor
    post:
      summary: Crear cliente
      description: Crea un nuevo cliente. Campos `name`, `email` y `phone` son obligatorios.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteInput'
      responses:
        '201':
          description: Cliente creado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cliente'
        '400':
          description: Petición inválida (faltan campos)
        '500':
          description: Error del servidor
  /api/clientes/{id}:
    parameters:
      - name: id
        in: path
        description: Identificador UUID público del cliente
        required: true
        schema:
          type: string
    put:
      summary: Actualizar cliente
      description: Actualiza un cliente por su `id`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteInput'
      responses:
        '200':
          description: Cliente actualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cliente'
        '404':
          description: Cliente no encontrado
        '500':
          description: Error del servidor
    delete:
      summary: Eliminar cliente
      description: Elimina un cliente por su `id`.
      responses:
        '200':
          description: Cliente eliminado
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Eliminado
        '404':
          description: Cliente no encontrado
        '500':
          description: Error del servidor
components:
  schemas:
    Cliente:
      type: object
      properties:
        id:
          type: string
          description: UUID público
          example: "c56a4180-65aa-42ec-a945-5fd21dec0538"
        name:
          type: string
          example: "Juan Pérez"
        email:
          type: string
          format: email
          example: "juan@ejemplo.com"
        phone:
          type: string
          example: "12345678"
        company:
          type: string
          example: "Tienda Central"
        address:
          type: string
          example: "Calle Falsa 123"
        notes:
          type: string
          example: "Entrega por la mañana"
        productType:
          type: string
          example: "Garrafón"
        expectedQuantity:
          type: integer
          example: 5
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - email
        - phone
    ClienteInput:
      type: object
      properties:
        name:
          type: string
          example: "María López"
        email:
          type: string
          format: email
          example: "maria@ejemplo.com"
        phone:
          type: string
          example: "87654321"
        company:
          type: string
          example: "Sucursal Norte"
        address:
          type: string
          example: "Av. Siempre Viva 742"
        notes:
          type: string
          example: "Prefiere horario de tarde"
        productType:
          type: string
          example: "Garrafón"
        expectedQuantity:
          type: integer
          example: 3
      required:
        - name
        - email
        - phone

  # Endpoints para usuarios (autenticación / gestión de usuarios)
  /api/users:
    post:
      summary: Registrar nuevo usuario
      description: Crea un usuario. Si implementas autenticación, guarda contraseña hasheada.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserInput'
      responses:
        '201':
          description: Usuario creado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Petición inválida

  /api/users/login:
    post:
      summary: Login de usuario
      description: Autentica usuario y devuelve token (si está implementado).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginInput'
      responses:
        '200':
          description: Autenticación exitosa
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Credenciales inválidas

  /api/users/{id}:
    parameters:
      - name: id
        in: path
        description: Identificador público del usuario
        required: true
        schema:
          type: string
    get:
      summary: Obtener usuario por id
      description: Devuelve información pública del usuario (no incluye password).
      responses:
        '200':
          description: Usuario
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          description: Usuario no encontrado

components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: string
          example: "u_123456"
        name:
          type: string
          example: "Admin"
        email:
          type: string
          format: email
          example: "admin@ejemplo.com"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - email

    UserInput:
      type: object
      properties:
        name:
          type: string
          example: "Admin"
        email:
          type: string
          format: email
          example: "admin@ejemplo.com"
        password:
          type: string
          format: password
      required:
        - name
        - email
        - password

    LoginInput:
      type: object
      properties:
        email:
          type: string
          format: email
          example: "admin@ejemplo.com"
        password:
          type: string
          format: password
      required:
        - email
        - password

    AuthResponse:
      type: object
      properties:
        token:
          type: string
          description: Token JWT u otro token de sesión
        user:
          $ref: '#/components/schemas/User'
